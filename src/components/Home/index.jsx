import styled from "styled-components";

const Home = () => <HomeWrapper>Home</HomeWrapper>;

export default Home;

const HomeWrapper = styled.div`
  align-items: center;
  display: flex;
  position: relative;
  width: 100vw;
`;
